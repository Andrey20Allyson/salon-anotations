import { StyleSheet } from "react-native";

export const {
  body,
} = StyleSheet.create({
  body: {
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#012',
    flex: 1,
  },
});