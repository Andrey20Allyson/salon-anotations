import { createStyle } from "../../utils/native-style";

export const screen = createStyle({
  flex: 1,
  backgroundColor: '#171717',
  justifyContent: 'center',
  alignItems: 'center',
});

export const container = createStyle({
  backgroundColor: '#fff1',
  justifyContent: 'center',
  alignItems: 'center',
  padding: 20,
  borderRadius: 15,
  marginTop: 20,
  marginBottom: 20,
});